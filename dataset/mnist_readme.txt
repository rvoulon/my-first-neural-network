The full dataset in CSV format is available at: http://pjreddie.com/projects/mnist-in-csv/

If you're adding them to your own project, make sure that .gitignore knows about them... your `git push` will fail if you commit them to your repo.
